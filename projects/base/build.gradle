import net.neoforged.neodev.NeoDevPlugin

plugins {
    id 'java-library'
    id 'net.neoforged.neodev'
}

plugins.getPlugin(NeoDevPlugin).configureBase(project)

dependencies {
    implementation("net.neoforged:neoform:${project.minecraft_version}-${project.neoform_version}") {
        capabilities {
            requireCapability 'net.neoforged:neoform-dependencies'
        }
        endorseStrictVersions()
    }
}
